<Page x:Class="Unbound.Logs"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Logs" Background="{DynamicResource BgColor}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="КОНСОЛЬ ДИАГНОСТИКИ (LOGS)" Foreground="{DynamicResource TextColor}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBox x:Name="LogTextBox" Grid.Row="1" Background="{DynamicResource CardBgColor}" Foreground="#00FF00" 
                 FontFamily="Consolas" FontSize="12" IsReadOnly="True" 
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Padding="8"/>
    </Grid>
</Page>