<Window x:Class="Unbound.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Unbound Hub" Height="480" Width="750" 
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent">

    <Border Background="{DynamicResource BgColor}" CornerRadius="8" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                    Background="{DynamicResource MenuBgColor}" 
                    BorderBrush="{DynamicResource BorderColor}" 
                    BorderThickness="0,0,0,1"
                    CornerRadius="7,7,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">

                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="15,0,0,0">
                        <Border Background="#4CAF50" Width="10" Height="10" CornerRadius="2" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="UNBOUND HUB" Foreground="{DynamicResource TextColor}" FontSize="12" FontWeight="SemiBold"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                        <Button x:Name="BtnMinimize" Content="─" Width="45" Height="31" 
                                Background="Transparent" Foreground="{DynamicResource TextColor}" 
                                BorderThickness="0" Focusable="False" Click="BtnMinimize_Click" 
                                Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>

                        <Button x:Name="BtnMaximize" Content="▢" Width="45" Height="31" 
                                Background="Transparent" Foreground="{DynamicResource TextColor}" 
                                BorderThickness="0" Focusable="False" Click="BtnMaximize_Click" 
                                Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>

                        <Button x:Name="BtnClose" Content="✕" Width="45" Height="31" 
                                Background="Transparent" Foreground="{DynamicResource TextColor}" 
                                BorderThickness="0" Focusable="False" Click="BtnClose_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="Bdr" Background="Transparent" CornerRadius="0,7,0,0">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Bdr" Property="Background" Value="#E81123"/>
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="0" Background="{DynamicResource MenuBgColor}" CornerRadius="0,0,0,7">
                <StackPanel>
                    <Button Content="🏠" Height="50" Click="Nav_Main_Click" Background="Transparent" Foreground="{DynamicResource TextColor}" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="🌐" Height="50" Click="Nav_Services_Click" Background="Transparent" Foreground="{DynamicResource TextColor}" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="⚙" Height="50" Click="Nav_Settings_Click" Background="Transparent" Foreground="{DynamicResource TextColor}" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="ℹ" Height="50" Click="Nav_About_Click" Background="Transparent" Foreground="{DynamicResource TextColor}" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Border>

            <Frame x:Name="MainFrame" Grid.Row="1" Grid.Column="1" 
                   NavigationUIVisibility="Hidden" 
                   Navigated="MainFrame_Navigated"/>
        </Grid>
    </Border>
</Window>